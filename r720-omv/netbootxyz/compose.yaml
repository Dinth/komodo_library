x-versions:
  netbootxyz: &netbootxyz-version "ghcr.io/netbootxyz/netbootxyz:0.7.6-nbxyz18"

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  netbootxyz:
    image: *netbootxyz-version
    container_name: netbootxyz
    hostname: netbootxyz
    ipc: private
    restart: unless-stopped
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - NET_BIND_SERVICE
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 512M
    environment:
      TZ: "${TZ}"
      PUID: "${DOCKER_PUID}"
      WEB_APP_PORT: "3010"
      NGINX_PORT: "85"
      TFTPD_OPTS: "--tftp-single-port"
    healthcheck:
      test: curl -sf --max-time 2 http://localhost:3010/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    ports:
      - 3010:3010/tcp   # Web configuration interface
      - 69:69/udp       # TFTP server (PXE boot)
      - 85:85/tcp       # HTTP server for assets
    volumes:
      - /opt/docker/netbootxyz/netbootxyz_config:/config:rw
      - /Data/IT/netbootxyz_assets:/assets:rw
      - /etc/localtime:/etc/localtime:ro
    networks:
      - netbootxyz
    logging: *default-logging
    labels:
      wud.watch: "true"
      wud.tag.include: '^\d+\.\d+\.\d+-nbxyz\d+$$'
      wud.display.name: 'netboot.xyz'
      wud.display.icon: 'mdi:server-network'
      wud.link.template: 'https://github.com/netbootxyz/docker-netbootxyz/releases/tag/$${major}.$${minor}.$${patch}'

networks:
  netbootxyz:
    name: netbootxyz
    driver: bridge
    external: false
