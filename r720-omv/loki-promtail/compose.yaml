x-versions:
  loki: &loki-version "grafana/loki:3.6.7"
  promtail: &promtail-version "grafana/promtail:3.5.8"
  fluentbit: &fluentbit-version "fluent/fluent-bit:4.2.3"
  promtail-socket-proxy: &promtail-socket-proxy-version "wollomatic/socket-proxy:1.11.3"
  alloy: &alloy-version "grafana/alloy:v1.13.2"

services:
  promtail-socket-proxy:
    image: *promtail-socket-proxy-version
    container_name: promtail-socket-proxy
    ipc: private
    command:
      - '-loglevel=info'
      - '-allowfrom=promtail,alloy'
      - '-listenip=0.0.0.0'
      - '-allowGET=/v1\..{1,2}/(version|containers/.*|networks|events)'
      - '-allowHEAD=/_ping'
      - '-watchdoginterval=1200'
      - '-stoponwatchdog'
      - '-shutdowngracetime=10'
      - '-allowhealthcheck'
    restart: unless-stopped
    read_only: true
    mem_limit: 64M
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges
    user: "65534:${DOCKER_SOCKET_GID}"
    healthcheck:
      test: ["CMD", "./healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 2
      start_period: 10s
    labels:
      wud.watch: "true"
      wud.tag.include: '^\d+\.\d+\.\d+$$'
      wud.link.template: "https://github.com/wollomatic/socket-proxy/releases/tag/$${major}.$${minor}.$${patch}"
      wud.display.name: 'Docker Socket Proxy - Promtail'
      wud.display.icon: 'mdi:connection'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - monitoring

  loki:
    container_name: loki
    image: *loki-version
    restart: unless-stopped
    user: ${DOCKER_PUID}:${DOCKER_PGID}
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    command: ["-config.file=/etc/loki/local-config.yml"]
    environment:
      TZ: "${TZ}"
    hostname: loki
    ipc: private
    networks:
      - monitoring
    ports: 
      - 3100:3100
    healthcheck:
      test: ["CMD", "/usr/bin/loki", "-health"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - /opt/docker/loki-promtail/loki-config.yml:/etc/loki/local-config.yml:ro
      - /opt/docker/loki-promtail/loki_storage:/loki
    labels:
      wud.watch: "true"
      wud.tag.include: '^\d+\.\d+\.\d+$$'
      wud.display.name: "Loki"
      wud.display.icon: "si:grafana"
      wud.link.template: 'https://github.com/grafana/loki/releases/tag/v$${major}.$${minor}.$${patch}'
      homepage.group: "Infrastructure"
      homepage.name: "Loki"
      homepage.icon: "grafana-loki.png"
      homepage.description: "Log Aggregation"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
  alloy:
    image: *alloy-version
    container_name: alloy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
    mem_limit: 512M # Alloy is efficient but needs more than the proxy
    cap_drop:
      - ALL
    cap_add:
      - DAC_READ_SEARCH # Allows reading logs without full root privileges
      - CHOWN           # Allows Alloy to manage its own internal positions file
    security_opt:
      - no-new-privileges  
    ports:
      - "1514:1514/udp"
      - "1514:1514/tcp"
    depends_on:
      loki:
        condition: service_healthy
      promtail-socket-proxy:
        condition: service_started
    command: [ "run", "--server.http.listen-addr=0.0.0.0:12345", "/etc/alloy/config.alloy", "--disable-reporting" ]
    environment:
      TZ: "${TZ}"
      # Alloy can use env vars in the .alloy config!
      HOSTNAME: "r720-omv" 
    healthcheck:
      test: ["CMD", "/usr/bin/alloy", "status"]
      interval: 30s
      timeout: 5s
      retries: 3
    volumes:
      - /opt/docker/loki-promtail/config.alloy:/etc/alloy/config.alloy:ro
      - /var/log:/var/log:ro
      - /var/log/journal:/var/log/journal:ro 
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /opt/docker/loki-promtail/alloy-storage:/var/lib/alloy
    
    labels:
      # What's Up Docker (WUD) Configuration
      wud.watch: "true"
      wud.tag.include: '^v\d+\.\d+\.\d+$$'
      wud.display.name: "Grafana Alloy"
      wud.display.icon: "si:grafana"
      wud.link.template: "https://github.com/grafana/alloy/releases/tag/$${tag}"
      # Homepage Dashboard Labels
      homepage.group: "Infrastructure"
      homepage.name: "Alloy"
      homepage.icon: "grafana-alloy.png"
      homepage.description: "Log & Metric Collector"
      homepage.widget.type: "prometheus"
      homepage.widget.url: "http://alloy:12345"
    
    networks:
      - monitoring      

  fluentbit:
    container_name: fluentbit
    image: *fluentbit-version
    restart: unless-stopped
    depends_on:
      loki:
        condition: service_healthy
    user: ${DOCKER_PUID}:${DOCKER_PGID}
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    environment:
      TZ: "${TZ}"
    hostname: fluentbit
    ipc: private
    ports:
#      - "1514:1514/udp"
#      - "1514:1514/tcp"
      - 2020:2020
    networks:
      - monitoring
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:2020/api/v1/health | grep -q 'ok'"]
      interval: 30s
      timeout: 5s
      retries: 3
    volumes:
      - /opt/docker/loki-promtail/fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf:ro
      - /opt/docker/loki-promtail/parsers.conf:/fluent-bit/etc/parsers-custom.conf:ro
      - /etc/machine-id:/etc/machine-id:ro
      - /var/log:/var/log:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    labels:
      wud.watch: "true"
      wud.tag.include: '^\d+\.\d+\.\d+$$'
      wud.display.name: "Fluent Bit"
      wud.display.icon: "mdi:pipe"
      wud.link.template: 'https://github.com/fluent/fluent-bit/releases/tag/v$${major}.$${minor}.$${patch}'
      homepage.group: "Infrastructure"
      homepage.name: "Fluent Bit"
      homepage.icon: "fluentbit.png"
      homepage.description: "Log Processor"
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"

networks:
  monitoring:
    name: monitoring
    driver: bridge
    external: true