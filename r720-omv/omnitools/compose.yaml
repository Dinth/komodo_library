services:
  omnitools:
    image: iib0011/omni-tools:latest
    container_name: omnitools
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whoami.rule=Host('omnitools.${DOMAIN_NAME}')"
      - "traefik.http.routers.whoami.entrypoints=websecure"
      - "traefik.http.routers.whoami.tls=true"
      - "traefik.http.routers.whoami.tls.certresolver=letencrypt"
      - "traefik.http.services.whoami.loadbalancer.server.port=8586"
    ports:
      - "8586:80"
    networks:
      - omnitools
networks:
  omnitools:
    name: omnitools
    driver: bridge
    external: false