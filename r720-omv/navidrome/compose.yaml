services:
  navidrome:
    user: "${MEDIA_PUID}:${MEDIA_PGID}"
    cap_add:
      - AUDIT_WRITE
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - FSETID
      - KILL
      - MKNOD
      - NET_BIND_SERVICE
      - NET_RAW
      - SETFCAP
      - SETGID
      - SETPCAP
      - SETUID
      - SYS_CHROOT
    cap_drop:
      - ALL
    container_name: navidrome
    entrypoint: ['/app/navidrome']
    hostname: navidrome
    image: deluan/navidrome:latest
    ipc: private
    logging:
      driver: json-file
      options:
        max-file: "3"
        max-size: 10m
    networks:
      - navidrome
    ports:
      - 4533:4533/tcp
    labels:
      traefik.enable: "true"
      traefik.http.routers.whoami.entrypoints: websecure
      traefik.http.routers.whoami.rule: Host('music.${DOMAIN_NAME}')
      traefik.http.routers.whoami.tls: "true"
      traefik.http.routers.whoami.tls.certresolver: letencrypt
      traefik.http.services.whoami.loadbalancer.server.port: "4533"
    restart: unless-stopped
    volumes:
      - /opt/docker/navidrome/navidrome_data:/data
      - /Data/Music:/music:ro
    working_dir: /app
networks:
  navidrome:
    name: navidrome
    driver: bridge
    external: false