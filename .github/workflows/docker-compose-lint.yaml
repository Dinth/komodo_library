name: docker-compose-lint
on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
jobs:
  build:
    - name: Docker Compose Linter
      uses: sjafferali/docker-compose-lint-action@v0.1.2

