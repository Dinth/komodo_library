x-versions:
  adguardhome: &adguardhome-version "adguard/adguardhome:v0.108.0-b.84"

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  adguardhome:
    image: *adguardhome-version
    container_name: adguardhome
    hostname: adguardhome
    ipc: private
    restart: unless-stopped
    # AdGuard Home must run as root to bind port 53 and manage its internal process supervisor.
    # Security is enforced via cap_drop + minimal cap_add instead of a non-root user.
    cap_add:
      - NET_BIND_SERVICE  # bind privileged DNS port 53
      - CHOWN
      - DAC_OVERRIDE
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 256M
    environment:
      TZ: "${TZ}"
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "3000:3000/tcp"  # initial setup wizard; remains as admin UI after first run
    healthcheck:
      test: ["CMD", "wget", "-q", "-T2", "-O-", "http://localhost:3000/control/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    volumes:
      - /opt/docker/adguardhome/adguardhome_work:/opt/adguardhome/work
      - /opt/docker/adguardhome/adguardhome_conf:/opt/adguardhome/conf
      - /etc/localtime:/etc/localtime:ro
    logging: *default-logging
    labels:
      wud.watch: "true"
      wud.tag.include: '^v\d+\.\d+\.\d+-b\.\d+$$'
      wud.display.name: "AdGuard Home"
      wud.link.template: 'https://github.com/AdguardTeam/AdGuardHome/releases/tag/v$${major}.$${minor}.$${patch}-$${prerelease}'
